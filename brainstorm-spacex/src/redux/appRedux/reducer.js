
const appState = {
    data: [],
    isLoading: false,
    isError: false
}

export const appReducer = (state = appState, { type, payload }) => {
    switch (type) {
        default: return state
    }
}