
const authState = {
    isAuth: false,
    isLoading: false,
    isError:false
}

export const authReducer = (state = authState, { type, payload }) => {
    switch (type) {
        default:return state
    }
}